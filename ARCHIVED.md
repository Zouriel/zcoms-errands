# ⚠️ This repository is archived

**Folded into `github.com/Zouriel/zcoms-agent` (errands are now part of the unified agent binary).**

It was folded into the **3-repo zcoms architecture** (June 2026):

- **`zcoms`** — comms foundation (`zc`, TDLib daemon, IPC, contacts) + published `client/`
- **`zcoms-agent`** — the AI layer (bridge + triage + errands + session manager + `agent.db`)
- **`zcoms-team`** / **`zcoms-console`** — modules on top

History is preserved here for reference; no further development happens in this repo.
